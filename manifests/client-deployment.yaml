apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: client
  template:
    metadata:
      labels:
        app: client
    spec:
      containers:
      - name: client
        # IMPORTANT: Replace with your Docker Hub username
        # The 'latest' tag will be replaced by our CI/CD pipeline
        image: nishanthhks/client:04914dd24ddc5e7dc96d0cb0724156723ae59153
        ports:
        - containerPort: 80