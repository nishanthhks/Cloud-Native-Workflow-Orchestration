apiVersion: apps/v1
kind: Deployment
metadata:
  name: server-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: server
  template:
    metadata:
      labels:
        app: server
    spec:
      containers:
      - name: server
        image: nishanthhks/server:b05ac53547293a07c2af8b3cbe7f5cbd4a23a6bb
        ports:
        - containerPort: 3000
        env:
          - name: JWT_SECRET
            valueFrom:
              secretKeyRef:
                name: server-secrets   # locate the Secret
                key: JWT_SECRET
          - name: DATABASE_URL
            valueFrom:
              secretKeyRef:
                name: server-secrets
                key: DATABASE_URL
